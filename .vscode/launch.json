{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}\\bin\\mb",
            "args": ["--allowInjection", "--configfile", "${workspaceFolder}\\bin\\mb.json"],
        }
    ]
}
